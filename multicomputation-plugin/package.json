{
  "name": "openclaw-plugin-multicomputation",
  "version": "1.0.0",
  "description": "Extract multiple cognitive observables from every LLM call — pattern cache, scars, competence signals, gradient deltas, memory relevance. One call, all observations. Zero extra cost.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "keywords": [
    "openclaw",
    "plugin",
    "cognitive",
    "multicomputation",
    "pattern-cache",
    "memory",
    "agent-learning"
  ],
  "author": "nnbiech95-cpu",
  "license": "MIT",
  "peerDependencies": {
    "openclaw": ">=0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
